import { createClient } from "@supabase/supabase-js";
const bucket = process.env.SUPABASE_BUCKET_ENROLLMENT_DOCUMENTS || "enrollment-documents";
async function main(){const client=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);const{data,error}=await client.storage.getBucket(bucket);if(!data){const created=await client.storage.createBucket(bucket,{public:false,fileSizeLimit:10*1024*1024,allowedMimeTypes:["application/pdf","image/jpeg","image/png"]});if(created.error)throw created.error;console.log(`Bucket privado creado: ${bucket}`)}else{if(error)throw error;await client.storage.updateBucket(bucket,{public:false,fileSizeLimit:10*1024*1024,allowedMimeTypes:["application/pdf","image/jpeg","image/png"]});console.log(`Bucket privado verificado: ${bucket}`)}}main();
