import { NextRequest, NextResponse } from "next/server"; import { removePendingCitizenDocument } from "@/features/enrollment-documents/services/enrollment-documents.server"; import { mapApiRouteError } from "@/lib/api/route-error"; import { requireAuth } from "@/lib/server-auth";
export async function DELETE(req: NextRequest, { params }: { params: Promise<{ id: string; documentId: string }> }) { try { const user = await requireAuth(req); const { id, documentId } = await params; await removePendingCitizenDocument(id, documentId, user.id); return NextResponse.json({ ok: true }); } catch (error) { return mapApiRouteError(error, "No pudimos retirar el documento."); } }
