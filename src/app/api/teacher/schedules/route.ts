import { NextRequest, NextResponse } from "next/server"; import { listTeacherSchedules } from "@/features/teacher/services/teacher.server"; import { mapApiRouteError } from "@/lib/api/route-error"; import { requireAuth, requirePermission } from "@/lib/server-auth";
export async function GET(req: NextRequest) { try { const user = await requireAuth(req); requirePermission(user, "activity_schedules", "ver"); return NextResponse.json({ data: await listTeacherSchedules(user.id) }); } catch (error) { return mapApiRouteError(error, "No pudimos cargar tus horarios."); } }
