import { RequestAccessPageShell } from "@/features/auth/request-access/components/RequestAccessPageShell";

export const metadata = {
  title: "Solicitar acceso | Portal ciudadano",
};

export default function RequestAccessPage() {
  return <RequestAccessPageShell />;
}
