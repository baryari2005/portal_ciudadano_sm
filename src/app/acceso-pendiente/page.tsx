import { AccountStatusPage } from "@/features/auth/components/AccountStatusPage";

export default function AccesoPendientePage() {
  return (
    <AccountStatusPage
      title="Acceso pendiente"
      message="Tu solicitud de acceso todavía está pendiente de aprobación."
    />
  );
}
