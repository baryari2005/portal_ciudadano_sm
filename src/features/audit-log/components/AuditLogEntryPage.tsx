"use client";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import{CatalogLoadingState,CatalogPageHeader}from"@/features/activity-catalogs/components/CatalogPrimitives";import{useAuditLogEntry}from"../hooks/useAuditLog";import{AuditLogDetail}from"./AuditLogDetail";export function AuditLogEntryPage({id}:{id:string}){const router=useRouter(),{data,loading,error}=useAuditLogEntry(id);if(loading)return <CatalogLoadingState label="registro de auditoría" fullPage/>;return <main className="min-h-full bg-[var(--brand-page)] p-4 sm:p-6 lg:p-8"><CatalogPageHeader title="Detalle de auditoría" description="Registro histórico de solo lectura." total={data?1:0}/><div className="mt-6 rounded-3xl border border-[var(--brand-border-soft)] bg-[var(--brand-panel)] p-6">{data?<AuditLogDetail entry={data}/>:<div><p>{error}</p><Button className="mt-4" onClick={()=>router.push("/audit-log")}>Volver al historial</Button></div>}</div></main>}
