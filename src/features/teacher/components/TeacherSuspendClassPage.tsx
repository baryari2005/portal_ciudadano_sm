"use client";
import { useEffect,useState } from "react";
import { useRouter } from "next/navigation";
import { CalendarClock,MapPin,PauseCircle,Users } from "lucide-react";
import { toast } from "sonner";
import { AdminFormHeader } from "@/components/layout/admin-form-page";
import { AdminFormCard,AdminFormField,AdminPageShell } from "@/components/shared/admin-patterns";
import { ConfirmDialog } from "@/components/ui/ConfirmDialog";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { CatalogLoadingState } from "@/features/activity-catalogs/components/CatalogPrimitives";
import type { ActivitySession } from "@/features/activity-sessions/types/activity-session.types";
import { getTeacherClassClient,suspendTeacherClassClient } from "../services/teacher.service";
export function TeacherSuspendClassPage({sessionId}:{sessionId:string}){const router=useRouter(),[item,setItem]=useState<ActivitySession|null>(null),[reason,setReason]=useState(""),[error,setError]=useState(""),[confirm,setConfirm]=useState(false),[saving,setSaving]=useState(false);useEffect(()=>{void getTeacherClassClient(sessionId).then(setItem).catch(()=>toast.error("No pudimos cargar la clase."))},[sessionId]);if(!item)return <CatalogLoadingState label="clase" fullPage/>;function validate(){const clean=reason.trim();if(clean.length<10){setError("El motivo debe tener al menos 10 caracteres.");return false}if(clean.length>500){setError("El motivo no puede superar los 500 caracteres.");return false}setError("");return true}async function suspend(){setSaving(true);try{await suspendTeacherClassClient(sessionId,reason.trim());toast.success("Clase suspendida y personas afectadas notificadas.");router.replace("/teacher/classes")}catch(caught:unknown){const message=typeof caught==="object"&&caught&&"response" in caught?((caught as {response?:{data?:{message?:string}}}).response?.data?.message):undefined;toast.error(message??"No pudimos suspender la clase.")}finally{setSaving(false)}}return <AdminPageShell><AdminFormHeader icon={PauseCircle} title="Suspender clase" description="Indicá el motivo por el que la clase no podrá realizarse."/><AdminFormCard title={item.activitySchedule.activity.name} description={`${item.date} · ${item.startTime} a ${item.endTime} · ${item.establishment.name}`}><div className="grid gap-4 sm:grid-cols-2"><AdminFormField label="Establecimiento" icon={MapPin}><p className="flex min-h-12 items-center rounded-xl border border-[var(--brand-border)] bg-white py-3 pl-11 pr-3 font-medium text-[var(--brand-ink)]">{item.establishment.name}{item.space?` · ${item.space}`:""}</p></AdminFormField><AdminFormField label="Personas afectadas" icon={Users}><p className="flex min-h-12 items-center rounded-xl border border-[var(--brand-border)] bg-white py-3 pl-11 pr-3 font-medium text-[var(--brand-ink)]">{item.enrollmentSummary.confirmedCount+item.enrollmentSummary.waitlistCount} personas serán notificadas</p></AdminFormField></div><AdminFormField label="Motivo de la suspensión" icon={CalendarClock} align="start" className="mt-5"><Textarea value={reason} onChange={event=>{setReason(event.target.value);if(error)setError("")}} maxLength={500} placeholder="Explicá brevemente por qué se suspende esta clase..." className="min-h-36 resize-none py-3 pl-11" aria-invalid={Boolean(error)}/>{error?<p className="mt-1 text-sm font-semibold text-red-700">{error}</p>:null}<p className="mt-1 text-right text-xs font-medium text-[var(--brand-muted)]">{reason.length}/500 caracteres</p></AdminFormField><div className="flex flex-col-reverse gap-3 border-t pt-5 sm:flex-row sm:justify-end"><Button variant="outline" onClick={()=>router.push("/teacher/classes")}>Cancelar</Button><Button onClick={()=>{if(validate())setConfirm(true)}}><PauseCircle/>Confirmar suspensión</Button></div></AdminFormCard><ConfirmDialog open={confirm} title="Confirmar suspensión" description="Se suspenderá únicamente esta clase y se notificará a las personas afectadas." confirmLabel="Suspender clase" loading={saving} onClose={()=>!saving&&setConfirm(false)} onConfirm={()=>void suspend()}/></AdminPageShell>}
