"use client";import { useCallback,useEffect,useState } from "react";import { listEnrollmentsClient } from "../services/enrollments.service";import type { Enrollment,EnrollmentFilters } from "../types/enrollment.types";
export function useEnrollments(filters:EnrollmentFilters){const[data,setData]=useState<Enrollment[]>([]);const[meta,setMeta]=useState({total:0,page:1,pageSize:8,pageCount:1});const[loading,setLoading]=useState(true);const[error,setError]=useState(false);const refresh=useCallback(async()=>{setLoading(true);setError(false);try{const response=await listEnrollmentsClient(filters);setData(response.data);setMeta(response.meta);}catch{setError(true);}finally{setLoading(false);}},[JSON.stringify(filters)]);useEffect(()=>{void refresh();},[refresh]);return{data,meta,loading,error,refresh};}
